<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.commonlink">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.CommonLink">
		<activity android:name=".MainActivity"/>
		<activity android:name=".LocationPageActivity"/>
		<activity android:name=".LocationActivity"/>
		<activity android:name=".MenuUpdateActivity"/>
		<activity android:name=".updateButtonsActivity"/>
		<activity android:name=".EventUpdateActivity"/>
		<activity android:name=".LoginActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

</manifest>